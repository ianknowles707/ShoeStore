<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>


    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:padding="@dimen/padding_normal"
        tools:context=".welcome.WelcomeFragment">


        <ImageView
            android:id="@+id/imageView"
            android:layout_width="@dimen/header_image"
            android:layout_height="@dimen/header_image"
            android:scaleType="centerCrop"
            android:src="@drawable/shoes_image"
            android:contentDescription="@string/shoes_image" />

        <TextView
            android:id="@+id/text_view_welcome"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_normal"
            android:fontFamily="@font/roboto"
            android:gravity="center_horizontal"
            android:padding="@dimen/padding_normal"
            android:textColor="@color/colorAccent"
            android:textSize="@dimen/text_heading"
            android:textStyle="bold"
            tools:text="New account created for user bob" />

        <TextView
            android:id="@+id/text_View_continue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_normal"
            android:fontFamily="@font/roboto"
            android:gravity="center_horizontal"
            android:textSize="@dimen/text_body"
            tools:text="Click below to view the user instructions for the shoe inventory application" />

        <Button
            android:id="@+id/button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_large"
            android:fontFamily="@font/roboto"
            android:text="@string/button_instructions"
            android:textSize="@dimen/text_heading"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/text_View_continue" />

    </LinearLayout>
</layout>